name: Basic Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Hello World from correct branch!"
      - run: mkdir -p attribution/docs/daily_reports
      - run: echo "Test file" > attribution/docs/daily_reports/test.txt
      - run: ls -la attribution/docs/daily_reports