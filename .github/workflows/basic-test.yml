name: Basic Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - run: echo "✅ Git checkout successful!"
      - run: pwd
      - run: ls -la
      - run: git branch
      - run: git status