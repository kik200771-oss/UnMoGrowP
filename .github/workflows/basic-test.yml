name: Basic Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Hello World"
      - run: mkdir -p test-dir
      - run: echo "Test file" > test-dir/test.txt
      - run: ls -la test-dir